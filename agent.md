# Agent 协作与工程开发约定

本文档是本仓库的人机协作开发约定，供 AI coding agent、Codex 与人工协作者统一遵守。

本文档不替代架构设计。涉及目录结构、依赖方向和模块职责时，以 `ARCHITECTURE.md` 为准；涉及当前阶段目标和开发边界时，以阶段计划文档为准。

## 1. 开始工作前

开始分析、改代码、写测试或更新文档前，按以下顺序读取上下文：

1. `agent.md`
2. `ARCHITECTURE.md`
3. `README.md`
4. `openspec/config.yaml` 以及当前 change 下的 proposal、specs、design、tasks（如果存在）
5. `docs/go agent system - 系统看板.md`
6. `docs/` 下当前阶段对应的更具体计划文档（如果存在）

如果具体阶段计划与总阶段计划存在差异，先识别差异，再按照更具体的当前阶段文档执行，并保持与架构基准一致。

未完成验证时，禁止直接声称“已经完成”或“已经通过验收”。

## 2. 当前架构基准

当前系统的分层、模块职责、依赖方向、目录边界、前端约束和演化规则全部以 `ARCHITECTURE.md` 为准。本文件不重复维护架构摘录，避免形成第二份实现依据。

新增代码不得重新引入 `app/api`、`app/services`、`app/repositories`、`app/schemas` 等已废弃的目录约定。需要新增目录或调整跨模块依赖时，先更新 `ARCHITECTURE.md` 并完成相应 Change 评审。

## 3. 当前阶段边界

判断当前应该做什么时，必须以阶段计划为准，不根据局部代码或单次对话自行扩大范围。

当前项目已经完成 Phase 1 和 Phase 2，正在进入以招标书为输入的 Phase 3 应用建设。推进方式是后端完成一段能力，再由前端匹配最小页面，并经过自动化测试和人工验证。

当前 Phase 3 的主链路是：

```text
招标书
  -> 招标书理解
  -> 生成当前招标书专属的投标书结构
  -> 使用知识库填写内容
  -> 人工确认
```

当前不提前建设：

- 通用银行投标模板
- 跨银行格式统一
- 没有真实招标书依据的自动写作
- 未经人工确认的正式投标文件提交
- 一次性完整多场景 Agent
- 没有真实编排需求时的 LangGraph 复杂流程

## 4. 注释与文档

- 新增复杂函数、类、关键流程、临时兼容逻辑和非直观业务规则时，补充必要的中文注释或中文说明。
- 注释重点说明“做什么”；只有在容易误解、存在兼容逻辑或有重要取舍时，才补充“为什么这样做”。
- 直白代码不要求逐行添加注释，避免用重复注释掩盖代码结构问题。
- 外部协议、字段名和专业术语可以保留英文原词，但解释应使用中文。
- 重要能力如果仅靠代码不易理解，应同步补充 `docs/` 下的中文说明。
- 前端用户文案、调试文案和错误提示优先使用中文。

## 5. RAG 与 Agent 约定

涉及知识库和 RAG 的改动，优先保证：

- 检索链路清晰
- 结果可解释
- 来源可追踪
- 结构可扩展
- 无足够证据时明确返回资料不足，不编造事实

`search` 与 `ask` 如果共享检索过程，应尽量复用同一套 retrieval pipeline。

LangChain / LangGraph 当前只是预留的 Agent 接入边界。只有出现多步骤编排、条件分支、失败回退或可恢复状态等真实需求时，才引入相应框架。

## 6. 测试与验收

- 后端改动至少补充对应的 `pytest` 测试。
- 前端改动至少执行 `npm run build`。
- 关键业务链路需要覆盖正常、缺失、失败和证据不足等重要分支。
- 外部模型、Embedding、OCR 和业务系统测试使用稳定替身、固定输出或明确跳过条件，不让自动化测试依赖不可控外部服务。
- 需要验证架构边界时，优先补充 AST 或依赖扫描测试，避免只凭目录名称判断架构是否正确。
- 机测和人测没有完成时，只能说明自动化验证结果，不能提前标记阶段验收完成。
- 因环境问题无法验证时，明确写出未验证项和原因。

常用检查命令：

```powershell
python -m pytest -q
ruff check app tests
python -m compileall -q app tests
cd frontend
npm run build
```

## 7. 配置、安全与敏感数据

- 配置集中维护在 `app/shared/config.py`；新增配置时同步检查环境变量示例和相关说明文件是否需要更新。
- 禁止将密钥、数据库密码、真实业务凭据或真实敏感资料提交到 Git。
- 测试和公开文档使用匿名样例、合成数据、内存数据或稳定 mock。
- 运行时文件写入 `.runtime/`，不得把运行产物、OCR 输出或解析产物作为测试资产提交。
- 文档只对敏感资料做概括性说明，不展开真实资料名称、数量、类别或内容。

## 8. 禁止事项

- 禁止按已经废弃的目录约定新增代码。
- 禁止让 Domain 依赖 HTTP、数据库、LLM 框架或具体基础设施。
- 禁止在路由、Schema 或前端组件中堆积复杂业务规则。
- 禁止把 SQL、HTTP、模型调用和复杂业务规则混写在同一个模块里。
- 禁止为了测试绕过架构边界或把测试替身放入生产代码。
- 禁止吞掉异常、隐藏错误来源或无依据生成业务事实。
- 禁止为了“看起来高级”而过早引入复杂框架或复杂抽象。
- 禁止未经验证就声称“已经完成”或“已经通过验收”。

## 9. 协作原则

- 先保证正确，再考虑复杂。
- 先做最小闭环，再逐步演进。
- 改动必须可验证，避免只改代码不做检查。
- 优先保持现有主链路稳定，避免无必要的大改。
- 发现与架构基准不一致时，先区分当前功能问题、技术债和文档问题，再决定是否阻塞当前阶段。

## 10. OpenSpec 变更约定

- OpenSpec 用于描述需求变更、可观察行为和验收场景，不替代 `ARCHITECTURE.md`、阶段计划或本文件。
- 修改 HTTP 契约、数据库语义、业务规则、任务状态、跨模块边界、外部 Provider、RAG 证据行为或前后端联动能力前，必须先创建对应的 OpenSpec change。
- 存量项目采用 delta-first：只为当前要修改的能力编写增量规格，不为整个历史代码库补写规格。
- 开始实现前必须阅读当前 change 的全部已生成 artifacts；需求、设计或任务发生变化时，先更新 artifacts 再改代码。
- Scenario 必须能够映射到自动化测试、架构检查、前端构建或明确的人工验收证据；没有验证证据时，不得声明完成。
- OpenSpec 规格是业务行为契约，OpenSpace 或其他 Agent Skill 只能辅助执行，不能覆盖规格、架构边界和安全约束。

## 11. Git 提交信息约定

- 本项目使用中文分类前缀，不采用 `feat:`、`fix:` 等 Conventional Commits 前缀。
- 提交标题统一使用：`分类（模块）：简洁的中文动作描述`。
- 常用分类包括：`功能`、`修复`、`测试`、`文档`、`配置`、`归档` 和 `破坏性变更`。模块使用项目已有领域或工作边界，例如 `对话`、`聊天`、`会话`、`交互`、`附件`、`安全`、`架构`、`OpenSpec` 和 `LLM`。
- 提交标题必须包含分类、模块和中文描述；禁止使用没有分类模块的裸标题，也不要用英文分类替代中文分类。
- 示例：`功能（LLM）：增加请求限流治理`、`测试（对话）：补齐流式持久化契约`、`归档（聊天）：完成会话列表管理基础`。
- 提交前根据实际变更选择最准确的分类；一次提交不要混合多个互不相关的变更。
