# 第三阶段- 后端 F1 工作进度

本文档是第三阶段 F1 先锋接入阶段的执行与进度记录，负责记录 F1 的实际工作状态、实现证据、验证结果和遗留问题。

第三阶段的范围、阶段关系和总体完成标准以 [第三阶段工作计划.md](第三阶段工作计划.md) 为准；前端协作和页面验收记录在 [第三阶段 - 前端改造工作.md](第三阶段%20-%20前端改造工作.md) 中，本文件不替代总计划，也不扩大 F1 的工作范围。

## 1. F1 阶段定位

F1 的目标是按“技术底座 -> 独立 Agent 入口 -> 文件输入 -> 业务 Prompt”的顺序打通第三阶段的最小技术闭环：先完成 LLM 与 LangChain 接入，再建立招标书专属 Agent 入口，通过 HTTP 接收文件和查询状态，最后基于真实输入定义招标书 Prompt 与结构化输出，并确保实现符合 `agent.md` 和 `ARCHITECTURE.md` 的分层约束。

| 项目 | F1 范围 |
|---|---|
| 独立 Agent 端 | 招标书 Agent 的输入输出契约、调用入口和结构化结果返回 |
| 招标书输入 | 通过 HTTP 完成文件上传、文件准入、任务 ID、状态查询和原始来源保存 |
| LLM 接入 | 优先完成稳定的通用 Chat / Structured LLM Port、配置读取、失败契约、离线 Fake |
| LangChain 对接 | 优先完成基础 Adapter、模型绑定和 Structured Output 技术验证；业务 Prompt 在文件输入之后定义 |
| 投标书骨架 | 根据招标书格式生成只包含结构的投标书文档，不填写正文内容 |
| 前后端联调 | 独立 LLM 单轮技术验证，以及 Tender 上传、状态和骨架结果的页面联调 |
| 明确不做 | 完整招标书要求理解、内容填写、知识库引用、LangGraph、SubAgent、正式文件提交 |

## 2. Agent 应用模块族定位

招标书能力属于 Agent 应用模块族下的一个独立能力，不归入现有 `modules/online`、`modules/knowledge` 或 `modules/ingestion`。后续财务、风控等能力与招标书能力并列，互不因为同属 Agent 而直接耦合。

| 位置 | F1 职责 | 不负责的内容 |
|---|---|---|
| `app/modules/llm` | 通用 Chat / Structured Output 能力契约和调用用例，供多个 Agent 与应用复用 | 招标书业务规则、Agent 编排、Function Calling 和 MCP |
| `app/modules/agent/tender/application` | 招标书任务用例、文件接收流程、Prompt 业务流程、通用 LLM Port 调用和结果处理 | FastAPI、LangChain SDK、文件系统和模型客户端细节 |
| `app/modules/agent/tender/domain` | 招标书任务状态、输入输出契约、结构化业务结果和状态规则 | HTTP、数据库、LLM 框架和具体适配器 |
| `app/modules/agent/tender/ports` | 招标书任务存储等 Tender 专属端口 | 通用 LLM Port、具体文件存储、OpenAI SDK、LangChain 对象 |
| `app/interfaces/agent` | Agent 能力的外部协议适配和调用转换 | 直接编排业务流程、直接创建 LLM 客户端 |
| `app/interfaces/http` | 文件上传、任务状态 API 的 Schema、Assembler、依赖注入和异常映射 | 直接编排 Application 之外的模型或文件流程 |
| `app/infrastructure/llm` | 通用 LLM SDK、GLM 和 LangChain Adapter 的具体实现 | 招标书业务规则和 Prompt 业务意图 |
| `app/infrastructure/filesystem` | 招标书任务文件暂存的具体实现 | 决定招标书任务状态和业务流程 |
| `app/composition/agent.py` | 组装 Agent 能力及其具体适配器 | 在模块内部隐式创建跨层依赖 |

F1 计划中的目标边界如下：

```text
interfaces/http/llm ───────> modules/llm/application
                                 -> modules/llm/ports
                                 <- infrastructure/llm

interfaces/http/tender ────> modules/agent/tender/application
                                 -> modules/llm/ports / tender ports
                                 <- infrastructure/llm / filesystem

interfaces/agent ──────────> Agent Application / Capability

composition/ 负责统一组装
```

Agent 能力族的目标结构如下：

```text
app/modules/agent/
├── tender/                         # F1：招标书 Agent 能力
│   ├── application/
│   ├── domain/
│   └── ports/
├── finance/                        # 后续：财务 Agent 能力
└── risk/                           # 后续：风控 Agent 能力
```

## 3. 当前总状态

| 阶段 | 状态 | 说明 |
|---|---|---|
| F1 计划 | 已建立 | 范围已纳入 LLM 和 LangChain 最小接入 |
| Step 1：LLM 技术底座 | 已完成 | 通用 LLM Port、GLM / LangChain Adapter、配置、Fake、错误契约和专项测试已完成 |
| F1 实现 | 进行中 | F1-A 独立 LLM 技术验证已完成；Tender Agent、文件输入、业务 Prompt 和骨架生成尚未开始 |
| F1 验证 | 进行中 | F1-A 已完成自动化、联调和 Chrome 人工验收；其余 F1 切片尚未验证 |
| F1 阶段结论 | 未完成 | 不得进入 F2 |

状态值统一使用：`未开始`、`进行中`、`已完成`、`阻塞`、`暂缓`。

## 4. Step 1：LLM 技术底座与调用契约

Step 1 只建立后续 Agent 和招标书 Prompt 可以依赖的独立 LLM 能力，不实现招标书业务 Prompt，不接收文件，也不生成投标书骨架。通用边界、GLM / LangChain Adapter、独立 HTTP 技术验证入口和离线验证均已完成。

| 工作域 | 工作内容 | 计划落位 | 产出 |
|---|---|---|---|
| LLM 能力边界 | 明确 Chat 与 Structured Output 的输入、输出、模型标识、Prompt 版本和错误结果 | `modules/llm/ports` + `modules/llm/contracts.py` | 可被多个 Agent 复用的稳定 LLM Port |
| 调用配置 | 统一 API Key、Base URL、模型名、超时、温度和最大输出等配置来源 | `shared/config.py` + Composition Root | 可校验、可替换、不可泄露密钥的配置契约 |
| Provider 适配 | 通过公共 OpenAI-compatible Client Factory 复用现有 GLM 调用能力，定义真实模型与 Application 之间的转换边界 | `infrastructure/llm` | RAG 和 Agent 共享同一个 Client Factory，不向上暴露 SDK 对象 |
| LangChain 适配准备 | 确定 Chat Model、Structured Output 和调用异常如何映射到稳定 Port | `infrastructure/llm` | LangChain Adapter 的输入输出契约 |
| 结构化结果 | 定义最小技术验证结果，不代表招标书理解结果或企业事实 | `modules/llm/contracts.py` 或技术验证契约 | 可校验的通用结构化 Schema |
| 测试替身 | 提供固定成功、格式错误、超时、配置缺失和模型不可用等 Fake 场景 | `tests` | 不依赖真实外部模型的离线测试 |
| 依赖组装 | 让公共 Client Factory、生产 LLM、LangChain Adapter 和 Fake 均由 Composition Root 注入 | `composition/` | RAG、独立 Chat 和 Agent 可共享，生产/测试实现可切换 |

### Step 1 的调用边界

```text
LLM Application / Technical Probe
  -> Chat / Structured LLM Port
  <- Chat / Structured LLM Result

Tender Application
  -> Generic Structured LLM Port
  <- Structured LLM Result

Composition Root
  -> OpenAI-compatible Client Factory
  -> OpenAI-compatible / LangChain Adapter
  -> Fake LLM
```

| 调用方 | 可以做什么 | 不可以做什么 |
|---|---|---|
| LLM / Agent Application | 传入已经定义好的 Chat 或结构化请求，处理成功、失败和待处理结果 | 直接创建 `ChatOpenAI`、OpenAI Client 或读取环境变量 |
| LLM Port | 表达模型调用能力和稳定错误契约 | 依赖 LangChain 或具体供应商 SDK |
| Client Factory | 创建并缓存共享的 OpenAI-compatible Client，供 RAG 和 Agent Adapter 使用 | 决定招标书业务规则或直接编排 Application 流程 |
| LangChain Adapter | 创建 Chat Model、绑定 Structured Output、转换异常和结果 | 决定招标书业务规则或直接写入任务状态 |
| Fake LLM | 返回固定结构化结果或固定失败结果 | 访问真实网络、真实密钥或生产数据库 |

### Step 1 的技术验证用例

| 用例 | 输入 | 预期结果 |
|---|---|---|
| 正常文本调用 | 固定测试 Prompt 和用户消息 | 返回可展示的文本结果 |
| 正常结构化调用 | 固定测试 Prompt 和技术验证 Schema | 返回符合 Schema 的结构化结果 |
| 模型输出格式错误 | 无法通过 Schema 的模型结果 | 返回明确的结构化错误，不进入业务流程 |
| 模型调用失败 | Provider 异常或网络失败替身 | 映射为稳定的 LLM 调用失败结果 |
| 配置缺失 | 缺少 API Key、Base URL 或模型名 | 启动或调用前得到明确配置错误，不泄露敏感信息 |
| 超时 | 超时 Fake | 返回可识别的超时结果，保留重试边界 |
| 模型替换 | Fake 替换真实 Adapter | Application 测试无需修改业务代码即可运行 |

### Step 1 的明确边界

| 纳入 Step 1 | 不纳入 Step 1 |
|---|---|
| LLM Port、配置、真实 Adapter、LangChain Adapter、Fake 和契约测试 | 招标书文件接收 |
| Prompt 版本字段和结构化输出机制 | 招标书业务 Prompt 设计 |
| 独立 LLM 技术验证入口和 Agent Application 对 LLM Port 的依赖方式 | 投标书骨架生成 |
| 供应商错误、超时和格式错误映射 | LangGraph、SubAgent 和复杂工作流 |

Step 1 完成条件是：Application 可以只依赖稳定 Port，生产实现可以由 Composition Root 注入，Fake 可以覆盖主要成功与失败分支，且 Domain 不依赖 LangChain 或具体 LLM SDK。

## 5. 工作包进度

| 编号 | 工作包 | 主要产出 | 优先级 | 当前状态 | 完成证据 |
|---|---|---|---|---|---|
| F1-01 | LLM 技术端口 | `modules/llm/ports` 中的 Chat / Structured LLM Port、模型配置、Fake 和失败契约 | 高 | 已完成 | 通用 Port、兼容导出、Application 替换测试和失败映射测试 |
| F1-02 | LangChain 基础适配 | `infrastructure/llm` 中的模型绑定、基础 Prompt 载体、Structured Output 和输出转换 | 高 | 已完成 | Adapter 契约测试、格式错误测试 |
| F1-LLM-01 | LLM 单轮技术验证 | 独立 LLM Chat HTTP 入口，供前端验证真实 GLM / LangChain 调用；不包含上下文、工具和 Agent 编排 | 高 | 已完成 | HTTP 测试、前端联调、Chrome 页面人工验收和真实 HTTP 调用记录 |
| F1-03 | 招标书 Agent 端契约 | `interfaces/agent` 的 Agent 输入输出契约和 Application 调用转换 | 高 | 未开始 | Agent Adapter 契约测试 |
| F1-04 | Composition Root 接入 | `composition/` 统一组装通用 LLM、Agent、LangChain 生产实现和测试替身 | 高 | 进行中 | LLM 生产适配器已组装，Agent 入口尚未接入 |
| F1-05 | 招标书任务契约 | `modules/agent/tender/domain` 与 `application` 中的 Command、Result、任务状态、错误和重试边界 | 中 | 未开始 | Application / Domain 契约测试 |
| F1-06 | 招标书文件暂存 | `infrastructure/filesystem` 中的独立任务目录、Manifest、哈希和来源信息 | 中 | 未开始 | Repository 测试、路径安全测试 |
| F1-07 | 招标书 Prompt 契约 | `modules/agent/tender/application` 基于文件输入定义业务 Prompt、版本和最小结构化结果 Schema | 高 | 未开始 | Prompt 快照、Schema 校验和 Fake 输出测试 |
| F1-08 | 投标书骨架生成 | 根据结构化结果生成保留招标书格式的结构-only 文档，不写入正文事实 | 高 | 未开始 | 文档结构快照、格式检查和空内容断言 |
| F1-09 | HTTP / 前端辅助入口 | `interfaces/http` 提供 LLM 技术验证、上传、状态查询和骨架文档结果；前端展示回答、task_id、状态和错误信息 | 中 | 进行中 | LLM HTTP 测试、前端构建、Chrome 人工验证已完成；Tender 接口尚未实现 |
| F1-10 | 综合验证 | 后端回归测试、架构检查、编译检查和前后端联调验收 | 高 | 未开始 | 后端命令输出、联调记录和问题记录 |

优先级只表示执行顺序，不代表低优先级工作可以缺少。F1 的最终验收仍要求所有工作包完成。

## 6. 执行顺序与依赖

| 阶段 | 先完成的内容 | 后续内容 | 依赖关系 |
|---|---|---|---|
| 技术底座 | LLM Port、配置、Fake、LangChain Adapter | Agent 端技术契约和业务流程 | 没有稳定模型调用端口，不进入 Agent 业务 Prompt 设计 |
| LLM 联调 | 独立 LLM Chat HTTP 入口和前端单轮调用 | Tender Agent 入口 | 只验证模型调用，不引入上下文、工具或 Agent Chat |
| Agent 入口 | 招标书 Agent 输入输出契约、Adapter 和 Composition Root 组装 | 文件输入和 Prompt 调用 | Agent 端只能调用 Application，不能直接访问 SDK、Repository 或文件系统 |
| 文件输入 | 招标书任务契约、文件暂存、来源和状态 | 招标书 Prompt 契约 | Prompt 必须基于真实文件输入和可追踪来源定义 |
| Prompt 契约 | 招标书 Prompt、版本、最小结构化输出 Schema | 投标书骨架生成 | Prompt 输出必须先通过 Schema 校验，不能直接作为业务事实 |
| 骨架生成 | 根据结构化章节和格式约束生成结构-only 文档 | 内容填写和知识库引用 | 只能生成标题、顺序、目录、表格/附件位置和占位符，不生成正文事实 |
| 交互闭环 | HTTP 上传/状态接口、骨架文档结果、前端状态展示和 Agent 结果返回 | F1 验收 | 不建设完整工作台，不把 HTTP 当作 Agent 编排入口 |

## 7. F1 前后端联合交付切片

后端实现和前端实现保持独立记录，但 F1 按以下纵向切片联调和验收：

| 切片 | 后端交付 | 前端交付 | 联调结果 |
|---|---|---|---|
| F1-A：LLM 技术验证 | 通用 LLM Port、LangChain Adapter、`/api/v1/llm/chat` 和错误契约 | ChatPage 单轮真实调用、加载、错误和重试 | 已完成：前端拿到真实 GLM 文本回答，并完成 Chrome 人工验收 |
| F1-B：Tender 入口 | Agent 契约、任务创建 API 和文件准入 | Tender 上传页面和任务创建 | 未开始，当前前端为 UI Mock |
| F1-C：任务状态 | 任务存储、状态查询、失败和重试契约 | 任务详情、轮询和状态展示 | 未开始，当前前端为 UI Mock |
| F1-D：骨架结果 | Prompt、结构化结果和结构-only 骨架 | 章节树、占位符和下载 | 未开始，当前前端为 UI Mock |

知识库前端产品化可以与 F1 并行，但不作为 Tender F1 的前置依赖。前端具体实现、构建和页面证据记录在前端工作文档中。

## 8. 实现边界

| 层级 | 允许负责的内容 | 禁止承担的内容 |
|---|---|---|
| LLM Application / Port | 通用 Chat、Structured Output、模型结果和稳定错误契约 | 招标书业务规则、Agent 编排、Function Calling 和 MCP |
| Agent Interface | Agent 输入输出契约、调用转换、结构化结果返回 | 直接调用 LLM SDK、LangChain 对象、Repository 或文件系统 |
| HTTP / Frontend | 文件上传、状态查询、Schema、参数转换、依赖注入、状态展示和异常映射 | 直接调用 LLM、LangChain、Repository 或数据库 |
| Application | 招标书任务流程、Prompt 业务意图、调用端口和结果处理 | 依赖具体 SDK 或管理基础设施细节 |
| Domain / Ports | 任务状态、输入输出契约、LLM 能力端口 | 依赖 FastAPI、数据库、LangChain 或 LangGraph |
| Infrastructure | 文件存储、LLM SDK、LangChain Adapter 和具体配置 | 绕过 Application 直接改变业务状态 |
| Composition Root | 组装生产实现和测试替身 | 在各模块内隐式创建跨层依赖 |

招标书上传必须使用独立任务存储，不得调用制度知识库的正式入库流程，避免把招标书误写入现有知识库。

## 9. F1 验收门槛

| 验收域 | 通过标准 | 证据状态 |
|---|---|---|
| LLM 技术验证 | 独立 LLM 入口能够通过 LangChain 调用 GLM，前端能够完成单轮真实调用；不保存上下文、不调用工具 | 已完成 |
| Agent 端 | 招标书 Agent 能通过稳定契约调用 Application，并返回经过 Schema 校验的结构化结果 | 未验证 |
| 文件接收 | 合成或脱敏招标书能够上传，生成唯一 task_id，原始文件和来源可追踪 | 未验证 |
| 投标书骨架 | 能生成符合招标书章节顺序和基础格式的结构-only 文档；不包含企业事实、知识库内容或模型自由正文 | 未验证 |
| 任务状态 | 能查询 `pending`、`processing`、`completed`、`failed`、`waiting_confirmation` 等状态 | 未验证 |
| LLM 端口 | Application 只依赖稳定端口，Fake 能替换真实模型，配置缺失和调用失败有明确结果 | F1-A 已完成 |
| LangChain | Prompt 版本固定，结构化 Schema 校验生效，模型格式错误不会直接进入业务流程 | F1-A 已完成 |
| 架构边界 | Domain 无 LangChain / LangGraph 依赖，HTTP 无直接模型调用，Composition Root 负责组装 | F1-A 已完成 |
| 自动化测试 | Application、HTTP、任务存储、LLM、LangChain 和失败分支测试通过 | F1-A 专项已通过；其余 F1 未验证 |
| 工程检查 | `ruff check app tests`、编译检查和前端 `npm run build` 通过 | F1-A 专项已通过 |
| 人工验证 | 完成一次上传、状态查询和失败展示的基本操作验证 | 未验证 |

## 10. 进度记录

| 日期 | 工作内容 | 结果 | 遗留问题 |
|---|---|---|---|
| 2026-07-22 | 完成 GLM 结构化 LLM Port、公共 OpenAI Client Factory、LangChain Adapter、配置和离线测试 | 技术实现和离线测试完成；按迭代一架构归位及独立 HTTP 验证尚未完成 | Agent 端、文件输入和业务 Prompt 尚未开始 |
| 2026-07-24 | 明确独立 LLM 层与前后端 F1 联调切片 | 增加 F1-A 至 F1-D 的联合交付口径；前后端文档保持分工并共享验收关系 | LLM 通用边界、独立 HTTP 入口和前端真实联调尚未开始 |
| 2026-07-24 | 启动 F1-A：独立 LLM 单轮技术验证 | 当前只推进通用 LLM Port、HTTP 调用和 ChatPage 真实联调 | 尚未进入 Tender Agent、文件任务、骨架生成和工具编排 |
| 2026-07-24 | 完成 F1-A 代码实现和真实接口验证 | 通用 Chat Application、GLM LangChain Chat Adapter、`/api/v1/llm/chat`、前端 API/Hook 和 ChatPage 已接入；专项测试、`ruff`、编译检查、前端构建通过，真实 GLM 请求返回 200；补充 PostgreSQL 连接超时，数据库不可用时服务可快速启动 | Chrome 页面人工验收待补充；完整 PostgreSQL 集成测试仍需数据库服务 |
| 2026-07-27 | 完成 F1-A Chrome 页面人工验收 | Chrome 中验证页面加载、真实 GLM 回答、请求加载态、后端失败提示和“重试”恢复；回答显示 `glm-4.7`、耗时和 token 指标 | 完整 F1 仍需 Tender Agent、文件任务、骨架生成和数据库集成验证 |

## 11. F1 完成结论

| 结论项 | 当前结论 |
|---|---|
| 是否完成 F1 | 否 |
| 是否允许进入 F2 | 否 |
| 已完成切片 | F1-A：独立 LLM 单轮技术验证 |
| 当前下一步 | F1-B：Tender Agent 入口、文件准入和任务创建契约 |
| 进入条件 | 所有 F1 工作包完成，验收证据齐全，自动化与人工验证通过 |
| 进入 F2 前需要更新 | 本文档工作包状态、验收证据、遗留问题和阶段结论 |
