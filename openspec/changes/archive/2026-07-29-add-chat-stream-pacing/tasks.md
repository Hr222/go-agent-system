## 1. 受控展示队列

- [x] 1.1 扩展 `useDeltaRenderQueue`，将收到的增量按 Unicode 可读字符组加入队列并按固定展示节奏提交；完成条件：单个较长 `delta` 在多个绘制时机逐步显示，最终文本与原文本完全一致。
- [x] 1.2 增加积压自适应批量和计时器清理；完成条件：大量积压可在有限时间排空，开始新请求、取消或组件卸载后不再执行旧队列回调。

## 2. 生命周期保持

- [x] 2.1 保持 Chat 页面对完成、取消和失败的终止顺序；完成条件：终止状态与元数据仅在展示队列排空后更新，输出指示与重复发送限制在排空前持续有效。

## 3. 测试与验收

- [x] 3.1 扩展渲染队列测试；完成条件：覆盖中英文和 Emoji 字符组顺序、逐步显示、积压加速、完成前排空及取消后的回调清理。
- [x] 3.2 执行全量前端测试和 TypeScript 生产构建；完成条件：所有测试和构建通过。
- [x] 3.3 使用本地多片段 SSE 在浏览器验收；完成条件：真实页面能先显示部分文本并保持输出状态，再逐步显示完整文本和完成信息。
