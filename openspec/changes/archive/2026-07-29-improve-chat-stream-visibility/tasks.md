## 1. 流式 HTTP 分层

- [x] 1.1 在 `frontend/src/services/http` 新增专用 POST SSE 客户端，支持跨分块帧解析、HTTP 错误转换和 `AbortSignal`；完成条件：客户端按顺序交付 `meta`、`delta`、`complete`、`error` 和心跳事件，且不依赖 Chat 页面。
- [x] 1.2 将 Chat 流式业务 API 改为调用该客户端并删除重复的底层读取实现；完成条件：`ChatStreamError`、Chat 事件类型和取消语义保持兼容，页面组件不直接发起网络请求。

## 2. 可感知的增量渲染

- [x] 2.1 为当前助手消息实现按动画帧排空的 `delta` 队列；完成条件：同一次 SSE 读取中的多个增量仍按顺序经过独立渲染调度，最终文本与全部 `delta` 拼接结果一致。
- [x] 2.2 调整 Chat 页面的连接中与输出中展示，增加持续可见的输出指示；完成条件：发送后立即显示连接状态，首个增量后切换为输出状态，完成、取消和失败后停止指示。
- [x] 2.3 处理完成、取消和失败时的待渲染片段与消息元数据；完成条件：先排空本地已接收队列再提交终止状态，已接收文本不会丢失，模型和 Token 信息只在正常完成后显示。

## 3. 验证与验收

- [x] 3.1 更新流式 HTTP 客户端测试；完成条件：覆盖跨网络分块的 SSE、同一读取批次内的多个 `delta`、服务端 `error` 和 `AbortSignal`。
- [x] 3.2 增加渲染队列与 Chat 生命周期测试；完成条件：覆盖按帧追加、完成前排空、取消保留内容、部分失败保留内容、显式重试和重复发送限制。
- [x] 3.3 执行 TypeScript 严格检查与生产构建，并进行浏览器人工验收；完成条件：构建通过，慢首片段显示连接状态，多片段回答显示逐步文本，且浏览器 Network 中仍为 `text/event-stream`。
