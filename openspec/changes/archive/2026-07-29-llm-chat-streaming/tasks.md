## 1. 后端流式应用能力

- [x] 1.1 增加流式 LLM Port 和 Application 契约，并复用现有消息规范化规则；完成条件：合法消息可以进入流式应用，空消息和空白消息分别返回约定错误。（对应 Requirement：系统提供独立的单轮 LLM 对话契约、系统校验并规范化对话输入）
- [x] 1.2 增加 GLM Provider 的原生异步流式适配；完成条件：Provider 增量内容可以转换为内部流式片段，模型标识和可用 Token 用量可以在结束时传递。（对应 Scenario：有效消息建立流式响应）
- [x] 1.3 保持现有同步 `ChatLlmPort`、`ChatApplication.execute()` 和 JSON 调用路径不变；完成条件：现有同步单元测试和 HTTP 回归测试继续通过。（对应 Scenario：有效消息返回单轮 JSON 响应）

## 2. 流式 HTTP 接口

- [x] 2.1 新增 `POST /api/v1/llm/chat/stream` 路由，并返回正确的 SSE 响应头；完成条件：响应类型为 `text/event-stream`，并设置缓存和代理缓冲相关响应头。（对应 Requirement：系统提供独立的单轮 LLM 对话契约）
- [x] 2.2 实现 `meta`、`delta`、`complete`、`error` 和 `heartbeat` 事件的序列化；完成条件：合法请求能够按约定顺序发送事件，`delta` 内容保持生成顺序。（对应 Scenario：有效消息建立流式响应）
- [x] 2.3 实现流式接口的输入校验和错误映射；完成条件：空消息、空白消息、超长消息、未配置 Provider、上游失败和超时均返回对应状态或事件。（对应 Requirement：系统校验并规范化对话输入、系统映射 LLM 服务可用性和响应失败）

## 3. 取消、超时和资源治理

- [x] 3.1 实现客户端断开后的 Provider 流取消和资源释放；完成条件：客户端取消后不再继续消费 Provider 流，并释放并发配额。（对应 Scenario：客户端取消流式请求）
- [x] 3.2 增加 Provider 建连、首 Token、空闲和总请求超时；完成条件：不同超时类型能够被区分，并返回约定的错误状态或 `error` 事件。（对应 Scenario：流式响应开始前发生上游超时、流式响应开始后上游调用失败）
- [x] 3.3 增加流式请求并发限制；完成条件：超过容量的请求返回 `429`，且被拒绝的请求不会调用 Provider。（对应 Scenario：流式请求超过并发容量）
- [x] 3.4 将 Provider Client 和模型 Adapter 纳入应用级 Composition Root 生命周期管理；完成条件：请求之间可以复用应用级 Provider 资源，应用关闭时释放可关闭资源。（对应设计：Provider Client 使用应用生命周期管理）

## 4. 前端流式交互

- [x] 4.1 增加流式 HTTP 客户端和 SSE 分块解析器；完成条件：能够处理分块事件、跨分块文本、心跳事件和服务端错误，并支持 `AbortSignal`。（对应 Requirement：前端展示单轮对话的完整请求生命周期）
- [x] 4.2 增加 `useChatStream` Hook 和流式状态机；完成条件：支持 `idle`、`connecting`、`streaming`、`completed`、`cancelled` 和 `failed` 状态。（对应 Scenario：流式请求正在处理中、用户取消流式请求）
- [x] 4.3 修改 Chat 页面以支持助手占位消息和增量渲染；完成条件：用户消息发送后立即创建助手消息，收到 `delta` 后按顺序追加内容，完成后展示模型和 Token 信息。（对应 Scenario：用户发送有效消息并接收流式回答）
- [x] 4.4 增加取消、部分失败和显式重试交互；完成条件：取消或失败后保留已收到文本，用户点击重试才重新发起请求，系统不会自动重复调用。（对应 Scenario：流式回答产生部分内容后失败、用户显式重试失败请求）

## 5. 测试和架构验证

- [x] 5.1 增加流式 Application 和 Provider Adapter 测试；完成条件：覆盖输入规范化、增量顺序、空响应、Token 汇总和资源释放。（对应 Requirement：系统提供独立的单轮 LLM 对话契约、系统校验并规范化对话输入）
- [x] 5.2 增加 HTTP 契约测试；完成条件：覆盖 SSE 事件顺序、首个事件前错误、部分内容后错误、超时、取消、并发拒绝和旧 JSON 接口回归。（对应 Requirement：系统映射 LLM 服务可用性和响应失败）
- [x] 5.3 建立前端流式测试基线；完成条件：覆盖 SSE 解析、增量渲染、取消、失败、显式重试和重复发送限制。（对应 Requirement：前端展示单轮对话的完整请求生命周期）
- [x] 5.4 执行架构边界检查和前端构建检查；完成条件：后端依赖方向未被破坏，前端通过 TypeScript 严格检查和生产构建。（对应设计：保持同步 Port 和 Application 独立、前后端分层）

## 6. 运行环境和最终验收

- [x] 6.1 增加流式代理和部署配置说明；完成条件：代理关闭响应缓冲，配置连接保持、超时和心跳，并完成代理后的首 Token 验证。（对应设计：生产代理和响应头）
- [x] 6.2 增加流式日志、指标和敏感信息检查；完成条件：记录请求阶段、耗时、错误码和活跃流数，不记录 Prompt、增量文本、鉴权信息或 API Key。（对应设计：日志、指标和敏感信息）
- [x] 6.3 完成慢 Provider、慢客户端、浏览器中止、Provider 超时和并发上限验收；完成条件：记录测试命令、测试结果和人工验收现象。（对应 Requirement：系统映射 LLM 服务可用性和响应失败）
