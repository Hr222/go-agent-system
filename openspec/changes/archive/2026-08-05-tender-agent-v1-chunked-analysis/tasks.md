## 1. 分块契约与预算模型

- [x] 1.1 定义文档分块计划、章节路径、证据引用、Token/字符预算、局部提取结果和归并批次的业务模型；完成条件：模型能表达段落、表格、跨块关系、待确认项和失败批次，并通过独立校验测试。
- [x] 1.2 定义 Chunk Planner、局部分析器和全局归并器的 Port，明确 Provider 一次只接收受控请求；完成条件：Tender Application 依赖 Port，HTTP/MCP 和 DOCX Adapter 不直接感知模型 SDK 或批次实现。
- [x] 1.3 定义分块 Prompt、局部结果 Prompt、归并 Prompt 及版本标识；完成条件：Prompt 明确只能使用当前证据、不编造公司事实、局部阶段不决定全局单卷/多卷，并有 Fake LLM 断言。

## 2. DOCX 证据索引与语义分块

- [x] 2.1 扩展 DOCX Reader 输出标题路径、块顺序、表格表头、附件/模板标识和解压资源统计；完成条件：现有证据 ID 保持兼容，段落、表格和模板信息可供 Chunk Planner 使用。
- [x] 2.2 实现按章节边界、连续块和表格行组生成有限大小分块的 Planner；完成条件：普通文本不超过配置预算，表格超预算时重复表头，所有分块可回溯原始 evidence_id，且不依赖 LLM。
- [x] 2.3 增加 50MB 正常上限、70MB 硬拒绝线、DOCX 解压总量/文件条目/压缩比检查；完成条件：超限文件在 LLM、渲染前被拒绝，正常文件和压缩异常均有稳定错误与清理测试。
- [x] 2.4 为单卷、多卷、表格、附件、跨章节和超预算样本补充分块单元测试；完成条件：测试验证分块顺序、边界、表头保留、证据覆盖和无静默截断。

## 3. 分块提取与全局归并

- [x] 3.1 实现 Tender Application 的“索引 - 分块提取 - 结果压缩 - 有界归并 - 最终 Schema 校验”主链路；完成条件：一次 Application 调用能处理多个分块，并保持现有 `TenderGenerateSkeletonResult` 输出契约。
- [x] 3.2 实现分块局部要求提取和证据校验；完成条件：局部结果只能引用所属文档证据，缺少证据或格式无效时返回可断言的业务错误，不生成未验证骨架。
- [x] 3.3 实现有界树形归并、去重、冲突保留、单卷/多卷全局判断和服务交付成果区分；完成条件：Fake 场景覆盖单卷、多卷、跨块冲突、待确认和归并预算超限。
- [x] 3.4 实现分块阶段的有限重试、超时、总调用数/Token 预算和客户端中断清理；完成条件：失败批次有诊断 ID，稳定错误不泄露原文/Key/临时路径，所有临时资源释放。
- [x] 3.5 将 GLM 结构化适配器固定为受控 JSON Object 请求和本地 Pydantic 校验，避免调用不兼容的 `chat.completions.parse`；完成条件：真实兼容性 smoke 与 Fake Adapter 测试通过，普通 Chat 适配器行为不受影响。

## 4. 外部契约和组合根兼容

- [x] 4.1 在 Composition Root 组装 Chunk Planner、分块 LLM、归并 LLM 和 Tender Application；完成条件：Fake 容器可注入，真实容器可构造且不需要数据库会话。
- [x] 4.2 保持 HTTP `/api/v1/agents/tender/skeleton` 和 MCP `tender.generate_bid_skeleton` 输入输出兼容，增加分块限制错误映射；完成条件：成功、输入超限、分块失败和归并失败的 HTTP/MCP 测试通过，现有前端无需读取内部批次。
- [x] 4.3 保持请求级资源边界和日志脱敏；完成条件：成功、失败和客户端中断均清理临时文件，日志只含阶段/批次/耗时/异常类型，不含招标正文、用户关注点、API Key 或生成文件内容。

## 5. 验收与回归

- [x] 5.1 使用 Fake LLM 完成分块链路、结构化 JSON、证据追溯、输出渲染和错误分支测试；完成条件：后端相关测试覆盖新增 Requirement 的主要场景。
- [x] 5.2 使用 GLM 最小 JSON Object smoke、单块真实调用和真实 Tender 小样本验证分阶段耗时与错误分类；完成条件：记录输入规模、调用次数、每阶段耗时和成功/失败原因，不记录敏感原文。
- [x] 5.3 使用当前 Demo 排除 `_skill_test`，至少验证 10 个单卷和 10 个多卷样本的分块覆盖、全局归并、输出数量、证据追溯和 DOCX 可打开；完成条件：每个样本都有成功、待确认或失败记录，未验证项单独列出。
- [x] 5.4 执行后端 pytest、`ruff check app tests tools`、编译检查、前端测试和 `npm.cmd run build`；完成条件：失败项有明确记录，且新增诊断/探针入口可直接执行。
- [x] 5.5 更新 Tender Agent 验收清单、阶段进度和必要的 Change 文档；完成条件：明确 V1.1 分块范围、50MB/70MB 边界、未包含 V2/异步/大文件传输，并通过 OpenSpec 严格校验后才可归档。
