## 1. 通用纯文本契约

- [x] 1.1 在 `modules/llm` 定义单条和批量纯文本 Embedding Port 及结果、校验、失败契约；完成条件：任意应用模块无需导入 `ChunkItem` 即可声明向量请求。
- [x] 1.2 将当前 Embedding Provider Adapter 迁移为实现通用 Port；完成条件：批量结果数量、顺序和向量维度均可在运行时校验。
- [x] 1.3 调整 Ingestion 仅在自身边界完成 `ChunkItem` 与纯文本、向量结果的映射；完成条件：LLM Adapter 中不再引用 Ingestion 领域类型。

## 2. 验证

- [x] 2.1 增加单条、批量、空白文本、Provider 超时和无效结果的契约测试；完成条件：失败不会生成零向量或部分成功结果。
- [x] 2.2 运行现有入库向量化相关测试；完成条件：既有文档入库的向量化流程保持通过。
