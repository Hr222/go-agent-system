## 1. 候选索引与召回

- [x] 1.1 依赖已归档的 `add-generic-embedding-port` 和 `add-platform-capability-catalog`，定义能力检索元数据与独立内存索引；完成条件：索引对象不导入政策知识切块或 Repository。
- [x] 1.2 实现目录初始化和更新时的向量构建；完成条件：每条索引记录关联稳定能力代码并可定位其目录条目。
- [x] 1.3 实现按相似度排序、可配置候选上限的查询用例；完成条件：默认最多返回 5 个候选，结果携带能力代码和相关性信息。

## 2. 隔离与失败验证

- [x] 2.1 为空结果、阈值过滤、Embedding 失败和索引构建失败定义显式返回；完成条件：故障不会退化为全量目录或默认能力。
- [x] 2.2 增加候选排序、数量限制和政策索引隔离测试；完成条件：测试证明实现不访问 `kb_policy_chunk`、政策 Repository 或 RAG Pipeline。
