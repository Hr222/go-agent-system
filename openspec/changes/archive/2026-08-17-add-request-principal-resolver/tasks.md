## 1. 主体解析契约

- [x] 1.1 新增不可变的 `RequestPrincipal`，提供主体标识、认证状态、权限查询和稳定权限输出，供应用层作为唯一授权输入。
- [x] 1.2 新增协议无关的 `PrincipalResolverPort` 与请求上下文，使未来认证实现能够替换而不改变业务层契约。

## 2. HTTP 默认适配器

- [x] 2.1 实现匿名主体解析器和 HTTP 依赖注入入口；在未接入认证模块时固定返回零权限主体。
- [x] 2.2 确保匿名解析器不接受客户端 Header、请求参数或模型输出中的自报权限、角色、能力代码或分发键作为授权事实。

## 3. 架构说明与验证

- [x] 3.1 更新架构文档，明确能力目录声明所需权限、主体解析器提供当前可信权限，以及未来认证模块的替换边界。
- [x] 3.2 添加单元测试，覆盖匿名零权限、伪造权限无效、权限查询稳定性和可替换的 HTTP 解析器。
- [x] 3.3 运行目标 Ruff、目标测试、全量后端测试与 OpenSpec 严格校验，并记录可观察的通过结果。
