## 1. 受约束意图识别

- [x] 1.1 依赖已归档的 `add-intent-candidate-retrieval`，定义候选受限的 Structured LLM 请求与 `IntentAssessment` 结果；完成条件：结果只能引用已召回能力代码，并包含输入、缺失资料与澄清原因。
- [x] 1.2 实现目录状态、输入 Schema、权限和模型输出的二次校验；完成条件：目录外、禁用、无权限或无效模型结果只会返回澄清或未识别状态。

## 2. 显式确认边界

- [x] 2.1 定义待确认、已确认和已取消的确认提议模型及确定性规则；完成条件：确认、取消、空输入和候选为空无需调用 LLM 即可处理。
- [x] 2.2 生成仅含受控能力代码、标准化输入和固定分发键的批准结果；完成条件：该 Change 不导入 Agent Runtime、Online Use Case 或执行器。

## 3. 验证

- [x] 3.1 增加模型输出越界、资料缺失、能力禁用、权限不足、取消和未确认的测试；完成条件：所有这些状态均不发起任何业务能力调用。
- [x] 3.2 增加确认生命周期测试；完成条件：流程结束后不创建 Conversation 持久化、Task 状态或后台任务。
