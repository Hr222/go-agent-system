## 1. 策略与数据迁移

- [x] 1.1 在 Interaction Application 层实现确认策略分流：`never` 受控直行、`always` 创建提议、`conditional` 保守按批准处理；完成条件：分流只使用服务端目录、主体和输入复核结果，客户端不能影响策略。
- [x] 1.2 新增幂等迁移并同步目录种子，将 `chat.general` 的确认策略改为 `never`，其余现有能力保持不变；完成条件：新库与已有数据库均能得到一致策略。

## 2. 服务端交互流

- [x] 2.1 新增 Application 层交互流用例和 HTTP SSE Route：普通 Chat 输出 `meta`、`delta`、`complete`，批准分支输出 `approval_required`，其他受控结果输出 `result`；完成条件：HTTP 层不持有 Provider 或执行器，事件不泄漏分发键、完整输入和权限。
- [x] 2.2 将 `chat.general` 的无批准分支接入现有 `StreamingChatApplication`，保持流取消、并发槽释放和 Provider 错误映射；完成条件：仅在目录、权限和输入复核后开始模型流，流中错误不发送 `complete`。
- [x] 2.3 保持既有直接 Chat JSON/SSE 和确认接口兼容；完成条件：既有接口回归测试继续通过，需批准能力在收到 `approval_required` 前不执行。

## 3. Chat 流式体验

- [x] 3.1 新增前端交互流 API/Hook，解析 `meta`、`delta`、`complete`、`approval_required`、`result` 和 `error` 事件；完成条件：页面组件不直接创建网络请求，且继续使用专用流式 HTTP 客户端。
- [x] 3.2 将 Chat 页面改为消费交互流，恢复 `useDeltaRenderQueue` 的真实逐增量展示与取消；完成条件：普通对话无需批准即可显示连接中、输出中和完成状态，不使用完整结果后的伪动画。
- [x] 3.3 保留同一消息流中的批准卡、取消、澄清、拒绝和失败状态；完成条件：批准前不执行，内部能力代码和输入不会显示，失败可显式重试且不自动重复提交。

## 4. 验收与归档

- [x] 4.1 增加后端测试，覆盖 `chat.general` 无批准流、批准类能力、策略篡改、澄清、流错误和取消资源释放；完成条件：测试证明所有分流权与执行权均在服务端。
- [x] 4.2 增加前端测试并运行类型检查、构建和全量回归；完成条件：普通对话按增量显示，批准类请求不触发流式执行，现有页面不回归。
- [x] 4.3 使用真实浏览器验收普通对话的首字流式输出和批准类请求的确认边界；完成条件：普通问答无批准卡，业务动作仍先显示批准卡。
- [x] 4.4 完成安全、兼容性和人工验收后归档本 Change；完成条件：OpenSpec 严格校验通过、任务全部勾选，并记录本 Change 与前序统一意图网关 Change 的关系。
