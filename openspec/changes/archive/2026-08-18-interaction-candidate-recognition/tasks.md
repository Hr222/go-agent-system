## 1. 权限范围索引

- [x] 1.1 为候选召回定义权限集合的规范化范围键，并按范围保存独立的内存候选索引；完成条件：不同范围的索引条目互不覆盖。
- [x] 1.2 修改索引构建、就绪检查和查询契约，使三者使用同一个权限范围；完成条件：范围未就绪时返回 `INDEX_UNAVAILABLE`，构建失败只保留同范围的旧索引。

## 2. 调用链与回归测试

- [x] 2.1 更新现有候选召回和意图识别调用点，传递已解析的权限集合；完成条件：有权限请求不会隐式查询公共范围，现有最终识别和 Gateway 行为保持兼容。
- [x] 2.2 增加多权限范围测试，验证受限能力不会进入无权限范围的候选结果，且一方重建失败不会借用另一方索引；完成条件：候选代码与索引状态均按范围隔离。
- [x] 2.3 保留并运行候选排序、数量限制、阈值、Embedding 故障和政策知识隔离测试；完成条件：候选召回仍只输出候选，不访问政策索引或执行能力。

## 3. 验证

- [x] 3.1 运行 OpenSpec 严格校验、候选召回与交互目标测试、目标 Ruff、全量后端测试及 V1 Chat 回归；完成条件：P2.2 通过且已完成 P0/P1/P2.1 行为不回归。全量 Ruff 另有 3 项既有测试文件问题，不属于本 change。
