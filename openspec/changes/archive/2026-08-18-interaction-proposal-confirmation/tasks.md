## 1. 确认策略门控

- [x] 1.1 在创建提议时复核目录确认策略，仅允许 `always` 与 `conditional` 条目进入确认；完成条件：`never` 条目返回明确拒绝且不生成提议。
- [x] 1.2 保持目录可用性、权限和输入契约的服务端复核；完成条件：无效、禁用、无权或输入不合法的识别结果不产生待确认状态。

## 2. 短期提议存储

- [x] 2.1 将内存提议存储改为保存与返回深拷贝快照；完成条件：调用方修改原始或取回的提议对象不改变服务端状态。
- [x] 2.2 保持 TTL、主体绑定和原子一次消费行为；完成条件：过期、主体不匹配和重复消费均不返回可确认提议，主体不匹配不消耗原记录。

## 3. 回归与验证

- [x] 3.1 增加确认策略和提议快照测试，并运行既有确认、Gateway 与交互路由测试；完成条件：确认层自身不执行目标能力，旧接口行为兼容。
- [x] 3.2 运行 OpenSpec 严格校验、目标 Ruff、全量后端测试及 V1 Chat 回归；完成条件：P2.3 通过且 P0/P1/P2.1/P2.2 不回归。全量 Ruff 另有 3 项既有测试文件问题，不属于本 change。
