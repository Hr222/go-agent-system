## 1. 目录基线核对

- [x] 1.1 核对 `platform_capability` 表、领域对象、目录端口与 Repository 覆盖规格要求的能力字段和可见性过滤；完成条件：确认不需要新迁移、种子数据或 HTTP 契约调整。
- [x] 1.2 核对 Composition Root 的受控分发键静态绑定，以及 `AgentRuntime` 只从目录消费 `agent` 条目的行为；完成条件：目录不成为执行入口，且不存在平行 Agent 注册来源。

## 2. 边界回归保护

- [x] 2.1 在架构测试中覆盖目录领域、端口和应用服务不依赖 Dialogue Runtime 或 Agent Runtime 具体实现；完成条件：目录保持可被后续 Gateway 与编排能力消费的独立边界。
- [x] 2.2 运行已有目录集成测试，覆盖禁用/权限过滤、分发键静态校验和 Agent Runtime 类型过滤；完成条件：所有现有目录行为保持不变。

## 3. 验证

- [x] 3.1 运行 OpenSpec 严格校验、目标架构与目录测试、目标 Ruff 以及全量后端测试；完成条件：P2.1 规格和边界测试通过，P0/P1 与 V1 回归不受影响。全量 Ruff 另有 3 项既有测试文件问题，不属于本 change。
