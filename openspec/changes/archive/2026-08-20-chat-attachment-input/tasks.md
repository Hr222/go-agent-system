## 1. 对话请求传递

- [x] 1.1 扩展流客户端和 Hook 的可选输入，使已上传附件能够作为 `provided_inputs.source_document` 发送，同时无附件调用保持空输入。
- [x] 1.2 为流客户端补充请求体测试，验证只传递 `attachment_id` 且现有纯文本请求不变。

## 2. 聊天页附件交互

- [x] 2.1 在聊天编辑器组合单附件 `AttachmentPicker`，替换无行为的回形针按钮，并展示上传、失败、重试和移除状态。
- [x] 2.2 在上传未完成、流式回复或确认操作期间限制发送和附件交互；上传完成后随发送动作构造安全引用。
- [x] 2.3 为聊天页补充上传后发送、安全引用传递、上传中限制和纯文本回归测试。

## 3. 验证与归档

- [x] 3.1 运行前端测试和生产构建，并执行浏览器人工验收：选择 DOCX、上传、发起对话、观察确认卡。
- [x] 3.2 运行全量后端测试、静态检查、OpenSpec 严格校验；同步主规格、归档 Change 并分别提交功能和归档记录。
