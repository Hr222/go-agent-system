## 1. 临时资源恢复

- [x] 1.1 为文件系统附件存储写入并恢复经校验的元数据清单，使 TTL 内资源在后端重启后仍受原主体与会话约束；补充存储单元测试。

## 2. Agent 产物暂存

- [x] 2.1 在受控 Agent 分发边界暂存标准文件产物，以资源元数据替换原始字节，并在部分失败时清理已写入资源；补充分发与对话结果测试。

## 3. 下载接口

- [x] 3.1 提供绑定当前主体和 Conversation ID 的附件下载接口，返回附件响应并对缺失、过期及上下文不匹配返回统一受控错误；补充 HTTP 测试。

## 4. 聊天交付与验证

- [x] 4.1 在聊天 Agent 结果卡片渲染下载操作并传入当前会话作用域；补充前端测试。
- [x] 4.2 运行后端、前端和 OpenSpec 严格校验，并以真实 Tender 文件完成一次生成后下载验收。
