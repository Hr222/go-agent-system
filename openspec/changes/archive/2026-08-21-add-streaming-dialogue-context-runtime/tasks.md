## 1. 流式上下文运行时扩展

- [ ] 1.1 在既有流式 Conversation Runtime 的命令、事件/终态结果和依赖契约上补充上下文依赖，不新增第二个运行时。
- [ ] 1.2 保持既有 Access、user/assistant 写入和流式 LLM 调用路径，在其请求构造中加入分页历史、ContextBuilder 与 `ChatLlmRequest` 映射。
- [ ] 1.3 复用完整回答写入及预算/取消/上游/空回答/写入失败的无 assistant 失败闭合。

## 2. 验证

- [ ] 2.1 使用替身覆盖新会话、已有历史、顺序窗口、预算、完整回答和部分失败，并验证普通轮次只经过一个流式会话运行时。
- [ ] 2.2 运行相关 pytest、架构边界检查和 OpenSpec 严格校验。
