## 1. Conversation 领域与持久化

- [x] 1.1 在 Conversation 领域模型、摘要值对象/类型和读写端口中加入可空的 `topic_summary`，并覆盖空值、单行和 80 字符上限；以领域单元测试证明有效、清除和非法输入的行为。
- [x] 1.2 在 PostgreSQL ORM 模型、mapper、会话写入/列表读取 repository 及 composition root 中接入 `topic_summary`；以持久化测试证明字段可读写且不破坏现有 owner 隔离和分页排序。
- [x] 1.3 新增幂等数据库迁移，为现有 Conversation 增加可空 `topic_summary` 字段和长度约束，执行迁移验证历史记录保持 `NULL`，并补充迁移说明或检查脚本。

## 2. 首轮话题概括应用能力

- [x] 2.1 新增 Conversation Topic Summary Generator 端口及本地规则型实现：规范化空白、取首句并稳定截断为不超过 80 个字符的单行结果；以单元测试覆盖中文、换行、超长、空输入和生成器异常场景。
- [x] 2.2 将首轮 user Message 成功持久化后的 best-effort 概括流程接入现有对话写入/流式应用服务：仅在会话尚无概括时执行，生成失败时使用消息安全截断回退，不回滚消息或回答，已有或人工设置的概括不得被后续消息覆盖；以应用测试覆盖成功、回退、异常和后续消息场景。

## 3. 会话摘要接口与修改能力

- [x] 3.1 扩展会话摘要领域模型、列表服务、HTTP schema/assembler，使 `GET /api/v1/conversations` 返回可空 `topic_summary`，并保持当前主体过滤、排序、游标和最小响应；更新后端列表和 repository 测试。
- [x] 3.2 新增 owner-scoped 话题概括更新应用服务及 `PATCH /api/v1/conversations/{conversation_id}/topic-summary` 路由：只接受 `topic_summary` 字段，支持有效字符串和显式 `null` 清除，非法输入返回受控 4xx，越权修改被拒绝且不写入；补充服务、HTTP、访问边界和响应契约测试。
- [x] 3.3 在 composition root 注册概括器、更新服务、repository 和路由依赖，运行架构边界测试确认 interfaces 不直接访问 repository/SDK 且模块依赖方向保持不变。

## 4. Chat 前端会话管理

- [x] 4.1 扩展会话列表 API 类型、query/mutation hooks 和缓存更新逻辑，读取并保存 `topic_summary`，更新或清除成功后刷新当前列表；以 API/hook 测试验证请求体、响应和失败重试状态。
- [x] 4.2 修改 Chat 侧栏：优先展示 `topic_summary`，为空回退稳定日期标题，保留加载、空列表、失败和重试状态；以组件测试验证列表顺序、回退标题、新建/切换会话不回归。
- [x] 4.3 为当前会话增加话题概括编辑、保存和清除交互，保存失败时保留编辑内容并提供重试，不覆盖服务端已有值；以组件测试覆盖成功、清除、校验失败和网络失败场景。

## 5. 验证与验收

- [x] 5.1 运行后端全量测试、数据库/迁移验证和架构边界测试，确认新增行为与已有会话创建、列表、历史读取、流式持久化全部通过。
- [x] 5.2 运行前端全量测试和生产构建，确认 Chat 侧栏话题展示、编辑/清除和错误状态通过。
- [x] 5.3 启动本地后端与前端进行人工验收：首轮消息自动生成话题概括、后续消息不覆盖、手动修改/清除立即反映、越权修改被拒绝；记录可复现的验证结果。
