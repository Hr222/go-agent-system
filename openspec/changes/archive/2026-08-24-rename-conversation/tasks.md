## 1. Chat 重命名交互

- [x] 1.1 将会话更多菜单的“重命名”接入编辑器，支持加载当前话题概括、保存有效名称、空白清除和取消，且不触发会话切换。
- [x] 1.2 处理保存中、保存成功和保存失败状态：成功刷新列表缓存并反馈，失败保留草稿并显示错误。

## 2. 验证

- [x] 2.1 补充或更新 Chat 组件测试，覆盖菜单入口、保存名称、清除名称、取消和失败保留草稿。
- [x] 2.2 运行前端 Chat 测试、生产构建和 `openspec validate rename-conversation --strict --no-interactive`，并执行 `git diff --check`。
- [x] 2.3 在本地 Chat 页面人工验收重命名保存、清除、取消和失败提示；确认编辑操作不会切换会话。
