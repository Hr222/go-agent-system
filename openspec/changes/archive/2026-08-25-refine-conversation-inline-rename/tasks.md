## 1. 内联重命名交互

- [x] 1.1 将目标会话的标题渲染为内联编辑控件，复用既有草稿、保存、取消和失败状态，且编辑控件不触发会话切换。
- [x] 1.2 调整会话项布局样式，确保输入框、确认/取消图标、日期、置顶标识和菜单入口在窄侧栏内可用；移除侧栏底部共享编辑器。

## 2. 验证

- [x] 2.1 补充组件测试，覆盖仅目标项进入编辑、取消不请求、保存成功和失败保留草稿。
- [x] 2.2 运行相关前端测试、生产构建、OpenSpec 严格校验和 `git diff --check`，并在本地 Chat 页面人工验收首个会话的内联重命名。
