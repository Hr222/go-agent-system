## 1. 删除应用与持久化

- [x] 1.1 核查并完善当前主体范围的 Conversation 删除应用服务、写端口和 PostgreSQL repository，确保真实删除父记录，失败时回滚。
- [x] 1.2 在真实 PostgreSQL 测试 schema 中验证删除 Conversation 后消息和事件均由外键级联删除，并覆盖删除失败回滚。
- [x] 1.3 覆盖删除接口的成功、匿名、越权、无效 UUID 与不存在会话分支，确认拒绝路径不改变数据。

## 2. Chat 删除交互

- [x] 2.1 将会话菜单删除动作接入单项二次确认；取消确认时不得发送请求。
- [x] 2.2 删除成功后刷新列表并清理当前会话的本地状态；删除失败时保留会话并显示反馈。
- [x] 2.3 补充 Chat 组件测试，覆盖确认、取消、成功清理和失败保留。

## 3. 验证

- [x] 3.1 运行 Conversation、基础设施和架构测试，以及前端 Chat 测试和生产构建。
- [x] 3.2 执行专用真实数据库删除验收：创建带消息和事件的测试会话，调用删除用例后查询三张表确认均不存在。
- [x] 3.3 运行 `openspec validate delete-conversation --strict --no-interactive`、全库严格校验与 `git diff --check`。
- [ ] 3.4 在本地 Chat 页面人工验收删除确认、取消、成功和失败提示；不删除既有用户会话。
