## 1. 领域契约

- [x] 1.1 新增 `StructuredAgentCall`，校验非空调用/能力标识、对象输入、可选关联 ID 和未知字段。
- [x] 1.2 新增 `AgentCallResult` 与 `AgentCallError`，校验对象输出、受控错误字段和关联 ID。
- [x] 1.3 从 `interaction.domain` 和模块入口导出新契约，保持与现有 V1 对象并存。

## 2. 回归测试

- [x] 2.1 增加合法请求、成功结果和失败结果的序列化与关联 ID 测试。
- [x] 2.2 增加空标识、非对象数据、未知执行字段和错误细节字段的拒绝测试。
- [x] 2.3 运行目标测试、全量后端测试、OpenSpec 严格校验和目标 Ruff，确认现有交互/V1 链路不回归。
